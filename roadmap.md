# Roadmap

## In progress
- Practice Lab + Patterns library (this turn).

## Next
- Rename **Data Vault → Proof-of-Concept**. PoC cards link back to the source session, show how the PoC was arrived at, the repeatable path/steps, and a **Run Now** button that opens a picker: run in a Demo Sandbox, run inside the current active session, or (future) other targets.
- Ensure Practice Lab missions push their session data (findings, patterns, PoCs) into the same tables the rest of the app reads, so lab activity fully syncs everywhere.

## Later
- Extend Run Now with more destinations (scheduled runs, chained PoCs, export).
